<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"
        integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <link rel='stylesheet' href='/stylesheets/flights-index.css' />
    <script src="../../public/javascripts/background.js"></script>

    <title>Mongoose Flightz</title>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" onclick="generate()" href="/">Home</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/flights/new">Add<span class="sr-only">(current)</span></a>
                </li>
            </ul>
        </div>
    </nav>

    <div class="container">
        <table class="table table-striped">
            <thead>
                <tr class="table-primary">
                    <th class="align-middle">Remove</th>
                    <th class="align-middle">Airline</th>
                    <th class="align-middle">Flight #</th>
                    <th class="align-middle">Airport Code</th>
                    <th class="align-middle">Departs</th>
                    <th class="align-middle">Details</th>
                </tr>
            </thead>
            <tbody>
                <% flights.forEach(function(f) { %>
                    <tr>
                        <form action="/flights/<%= f._id %>?_method=DELETE" method="POST">
                            <td class="align-middle">
                                <button class="btn btn-danger" type="submit">X</button>
                            </td>
                            <td class="align-middle">
                                <%= f.property %>
                            </td>
                            <td class="align-middle">
                                <%= f.flightNo %>
                            </td>
                            <td class="align-middle">
                                <%= f.airport %>
                            </td>
                            <td class="align-middle">
                                <%= f.departs %>
                            </td>
                            <td class="align-middle">
                                <a class="btn btn-primary" href="/flights/<%= f._id %>">Details</a>
                            </td>
                        </form>
                    </tr>

                    <% }); %>

            </tbody>
        </table>

    </div>

</body>

</html>